package.Opendistro-async-search = {
    interfaces = (aes_7.7);

    build-system = download-prebuilt-plugin;
    build-tools = {
        aes_7.7 = {
            S3BinarySafeDownloader = 1.0;
            # For local development and consuming brazil dependencies
            GradleTrails = 3.0;
        };
    };

    # Use runtime-dependencies for when you want to bring in additional
    # packages when deploying.
    # Use dependencies instead if you intend for these dependencies to
    # be exported to other packages that build against you.
    dependencies = {
        aes_7.7 = {
        };
    };

    runtime-dependencies = {
        aes_7.7 = {
        };
    };

};
